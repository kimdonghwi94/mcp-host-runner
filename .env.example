# MCP Host Runner Environment Configuration

# 서버 설정
HOST=0.0.0.0
PORT=8001
ENVIRONMENT=production

# 로깅 설정
LOG_LEVEL=INFO
FILE_LOG_LEVEL=INFO
LOG_FILE=logs/mcp-runner.log
ENABLE_LOG_COLORS=true
CONSOLE_OUTPUT=true

# MCP 설정
MCP_SESSION_TIMEOUT=300
MCP_AUTO_CLEANUP=true
MCP_CACHE_ENABLED=true
MCP_CACHE_TTL=3600

# 보안 설정 (선택사항)
# API_KEY=your-secret-api-key-here

# CORS 설정
# CORS_ORIGINS=["https://your-domain.com", "https://another-domain.com"]

# 레이트 리밋 설정
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60

# Node.js/NPM 설정 (선택사항)
# NPM_CONFIG_CACHE=/tmp/.npm
# NODE_PATH=/usr/local/lib/node_modules

# 개발 모드 설정
# DEBUG=false